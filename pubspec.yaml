name: flutterclone
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
# Piano
  audioplayers: ^0.15.1
# English
  english_words: ^3.1.5
# Flutter Hooks
  flutter_hooks: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/a.wav  #<-- Add audio files
    - assets/c.wav
    - assets/e.wav
    - assets/f.wav
